<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.renkaen.cat_hospital.mapper.InStocksMapper">
    <!--1对多联表查inStocks和inbound-->
    <resultMap id="InStocksJoinInbound" type="com.renkaen.cat_hospital.bean.DTO.InStocksJoinInboundDTO" autoMapping="true">
        <result property="id" column="id"/>
        <collection property="inbound" column="id" select="joinInbound" />
    </resultMap>
    <select id="selectInStocksJoinInboundByType" resultMap="InStocksJoinInbound">
        select *
        from inStocks_table
        where type = #{type}
    </select>
    <select id="joinInbound" resultType="com.renkaen.cat_hospital.bean.PO.Inbound">
        select *
        from inbound_table
        where inStockId = #{id}
    </select>

    <!--根据权限id获取权限信息-->
    <select id="selectUserById" resultType="com.renkaen.cat_hospital.bean.PO.InStocks">
        select *
        from inStocks_table
        where id = #{id};
    </select>
<!--    获取所有的inStocks-->
    <select id="selectInStocksAll" resultType="com.renkaen.cat_hospital.bean.PO.InStocks">
        select *
        from inStocks_table;
    </select>

<!--    根据id更新instocks-->
    <update id="updateInStocksById" parameterType="com.renkaen.cat_hospital.bean.PO.InStocks">
        update inStocks_table
        set type = ifnull( #{inStocks.type} , type ),
            namae = ifnull( #{inStocks.namae} , namae ),
            keyTime = ifnull( #{inStocks.keyTime} , keyTime ),
            consumed = ifnull( #{inStocks.consumed} , consumed ),
            sellPrice = ifnull( #{inStocks.sellPrice} , sellPrice ),
            brand = ifnull( #{inStocks.brand} ,brand),
            amount = ifnull( #{inStocks.amount} ,amount)
        where id = #{id}
    </update>

    <insert id="insertInStocks" keyProperty="id" useGeneratedKeys="true">
        insert into inStocks_table (id,type,namae,keyTime,consumed,sellPrice,brand,amount)
        values (default, #{type}, #{namae}, #{keyTime}, #{consumed}, #{sellPrice}, #{brand}, #{amount})
    </insert>

    <delete id="deleteInStocksById">
        delete
        from inStocks_table
        where id = #{id}
    </delete>

</mapper>